{% extends get_admin_template('base_list_field', admin.code) %}

{% block field %}
    {% if value %}
        <span aria-hidden="true" style="font-size: 1.25rem; line-height: 1;">{{ value }}</span>
    {% endif %}
{% endblock %}
